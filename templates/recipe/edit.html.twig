{% extends 'base.html.twig' %}

{% block title %}Edit Recipe{% endblock %}

{% block body %}
    <h1 class=" text-center m-5">Modifier une recette</h1>

    {{ include('recipe/_form.html.twig', {'button_label': 'Mettre à jour'},{'class':'btn btn-primary'}) }}
    {{ include('recipe/_delete_form.html.twig') }}

    <a class=" btn btn-primary"href="{{ path('recipe_index') }}">Retourner aux recettes</a>

{% endblock %}
