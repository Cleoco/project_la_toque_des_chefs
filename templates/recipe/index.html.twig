{% extends 'base.html.twig' %}

{% block title %}RECETTES{% endblock %}

{% block body %}
    <h1 class="text-center m-5">RECETTES</h1>
        {# <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Title</th>
                    <th>Content</th>
                    <th>Note</th>
                    <th>Image</th>
                    <th>CreatedAt</th>
                    <th>UpdatedAt</th>
                    <th>actions</th>
                </tr>
            </thead> #}
            <tbody >
            {% for recipe in recipes %}
                <div class="row">
                    <div class="col-4 mb-4">
                        <tr>
                            {# <td>{{ recipe.id }}</td> #}
                            <td> <h2>{{ recipe.title }}</h2></td>
                            <td>{{ recipe.note }}</td>
                            <td><img class="col-12 mt-3 mb-3 p-0" src="{{ recipe.image }}" alt="" srcset=""></td>
                            {# <td>{{ recipe.createdAt ? recipe.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                            <td>{{ recipe.updatedAt ? recipe.updatedAt|date('Y-m-d H:i:s') : '' }}</td> #}
                            <td>
                                <a type="button" class="btn btn-danger" href="{{ path('recipe_show', {'id': recipe.id}) }}">show</a>
                                <a type="button" class="btn btn-secondary" href="{{ path('recipe_edit', {'id': recipe.id}) }}">edit</a>
                            </td>
                        </tr>
                    </div>
                </div>
            {% else %}
                <tr>
                    <td colspan="8">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <a type="button" class="btn btn-primary mt-5" href="{{ path('recipe_new') }}">Create new</a>
    
{% endblock %}
