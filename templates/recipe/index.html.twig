{% extends 'base.html.twig' %}

{% block title %}RECETTES{% endblock %}

{% block body %}
    <h1 class="text-center m-5">RECETTES</h1>
        {# <table class="table">
            <thead>
                <tr>
                    <th>Id</th>
                    <th>Title</th>
                    <th>Content</th>
                    <th>Note</th>
                    <th>Image</th>
                    <th>CreatedAt</th>
                    <th>UpdatedAt</th>
                    <th>actions</th>
                </tr>
            </thead> #}
            <tbody >
            {% for recipe in recipes %}
                <tr>
                    <td>{{ recipe.id }}</td>
                    <td>{{ recipe.title }}</td>
                    <td>{{ recipe.content }}</td>
                    <td>{{ recipe.note }}</td>
                    <td>{{ recipe.image }}</td>
                    <td>{{ recipe.createdAt ? recipe.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                    <td>{{ recipe.updatedAt ? recipe.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
                    <td>
                        <a href="{{ path('recipe_show', {'id': recipe.id}) }}">show</a>
                        <a href="{{ path('recipe_edit', {'id': recipe.id}) }}">edit</a>
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="8">no records found</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>

        <a type="button" class="btn btn-primary" href="{{ path('recipe_new') }}">Create new</a>
    
{% endblock %}
