{% extends 'base.html.twig' %}

{% block title %}Edit Advice{% endblock %}

{% block body %}

<style>
    .filet{
        margin : 50px auto 0 auto;
        width:80px;
        height:15px;
        background:#18BC9C;
    }

    h1{
        letter-spacing: 8px;
        text-transform: uppercase;
    }
    .title{
        position:absolute;
    }
</style>

    <h1 class=" text-center m-5">Modifier une fiche</h1>
    

    {{ include('advice/_form.html.twig', {'button_label': 'Mettre à jour'},{'class':'btn btn-secondary'}) }}
    {{ include('advice/_delete_form.html.twig') }}
    <a class=" btn btn-primary"href="{{ path('advice_index') }}">Retourner aux fiches</a></a>

{% endblock %}
